<h1></h1>
<h3></h3>
<div class="tweet">
  <p><%= @tweet.body %></p>
  <p></p>
</div>
      <% @tweet.images.each do |image| %>
        <%= image_tag image.image_url, size: "900x1260" %>
      <% end %>

<%= link_to "Tweet一覧に戻る", tweets_path %>
<div class="box1">
</div>
<%= link_to "編集する", edit_tweet_path(@tweet.id) %>
<%= button_to "削除する", tweet_path(@tweet.id),method: :delete,class: "sakujo" %>